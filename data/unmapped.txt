# Glyph indices the script uses that are not yet identified.
# NONE - every index the dialogue uses is now mapped (100%% coverage).
#
# Resolved by rendering the font directly rather than dumping in-game:
#   python tools/font.py export out/0004.bin glyphs/
# Any future unknown can be read the same way; no glyphdump pass needed.

index	uses
